name: random_forest
class: src.models.random_forest.RandomForestClassifier_
params:
  n_estimators: 200
  max_depth: null
  min_samples_leaf: 2
  class_weight: null
  n_jobs: -1
  random_state: null   # set from global seed at runtime
