name: mlp
class: src.models.mlp.MLPClassifier_
params:
  hidden_sizes: [64, 32]
  dropout: 0.3
  lr: 0.001
  max_epochs: 200
  batch_size: 256
  patience: 15          # early stopping patience
  device: null          # null = inherit from base.yaml
  random_state: null
